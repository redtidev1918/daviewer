# Changelog

本文件按主题记录用户可见的变更。逐提交历史与每个 patch 版本对应的具体提交见
[Releases](https://github.com/redtidev1918/daviewer/releases)。

## 0.2.182（下载按钮与长按确认）

### Fixed

- 下载区不再把「不能下载」画成一大块像主按钮的面板：原图不可下载时只保留一行
  紧凑原因说明；可保存展示图时给出带文字的描边下载按钮，完全不可下载时不再
  显示假的禁用按钮，避免误导。
- 长按图片下载前先弹「下载这张图片？」确认框，确认后才入队，降低误触；取消
  不下载，确认后仍会提示实际保存位置。

## 0.2.179（视频离开页面自动暂停）

### Fixed

- 详情页视频此前只在应用退到后台或组件销毁时暂停；跳转到其他页面（作者页、
  标签页等）覆盖详情页路由时，视频仍会在后台继续播放，空耗流量与电量。
  现在通过全局 `RouteObserver`（`RouteAware`）在路由被覆盖时暂停、返回时恢复播放。

## 0.2.178（作品更新日期与视频自动循环）

### Added

- 作品详情页标题下方新增发布时间与更新时间（相对时间，每分钟自动刷新）；
  被作者编辑过的作品会同时显示「更新于」，未编辑的只显示发布时间。
- 关注动态（Watched）页的作品列表和顶部作者头像条在每次刷新后按作品最新
  活动时间（更新时间优先，其次发布时间）稳定排序，无时间信息的条目排末尾。

### Changed

- 详情页视频加载后自动播放并循环播放，无需手动点击播放键。

## 0.2.177（推荐页返回体验优化）

### Fixed

- 详情页补充网页数据时更新 CSRF 不再重建推荐列表，返回推荐页后会保留原列表和
  滚动位置，避免阅读过程中内容突然刷新或跳回顶部。

### Changed

- 前台自动刷新改为保守策略：只有应用离开至少 10 分钟、推荐页当前可见且位于
  顶部附近时才静默更新。查看详情、浏览其他标签、短暂切换应用或深度滚动时均
  保持当前内容；手动下拉刷新不受影响。

## 0.2.176（滑动手势分层与全屏阅读优化）

### Fixed

- 详情页作品切换、详情内多图翻页和全屏大图翻页现在具有明确的手势所有权：
  全屏多图时只有内部 `PageView` 处理水平拖动，外层作品导航不再参与手势竞技场。
- 多图必须从首张或末张开始一次新的向外滑动，才会切换到相邻作品；从普通页面
  开始的同一次滑动以及滚动惯性都不会被提升为作品导航。
- 详情页打开全屏查看器时会传入当前作品的全部可查看图片和正确初始页，修复只能
  看到单张、无法左右翻动的问题。缩放时保持当前页，不再跳回第一张。
- 收藏夹与通知入口改为携带完整 `ArtworkBrowseSession`，修复从这些入口进入详情时
  上一个/下一个作品导航缺失的问题。
- CookieManager 临时不可用或读取为空时保留上一份有效 Cookie 快照，避免空数据
  覆盖恢复备份；快照改为临时文件落盘后原子替换，降低更新或进程中断导致损坏的风险。

### Changed

- 全屏查看器的返回、缩放和左右按钮打开后短暂显示，2 秒后自动隐藏；轻点画面可
  重新唤出。控件是否显示不影响滑动操作，减少对图片阅读的遮挡。
- 图片放大后由 `InteractiveViewer` 独占平移和缩放手势，不触发图片或作品切换。

## 0.2.175（全屏查看器翻页按钮）

### Added

- 全屏大图查看器新增左右翻页箭头按钮（半透明圆形），点击直接切换到
  上一个/下一个作品。这些按钮不依赖手势系统，在滑动手势被平台或
  其他组件拦截时仍然可用——作为滑动手势的可靠补充。

## 0.2.174（全屏查看器多图翻页修复）

### Fixed

- 多图作品在全屏查看器中缩放后 PageView 被丢弃（变成 `Center(child: image)`），
  导致无法继续翻页。现在缩放状态正确包裹当前页面的 InteractiveViewer，
  重置缩放后恢复 PageView 翻页。

## 0.2.173（滑动诊断日志）

### Added

- 全屏大图查看器的水平拖动开始与结束时记录诊断日志（回调是否存在、缩放状态、
  滑动距离与速度），用于定位滑动切换作品的用户端问题。

## 0.2.172（全屏大图滑动切换修复）

### Fixed

- 全屏查看器中 `InteractiveViewer` 即使在未缩放状态下也参与手势竞技场，
  抢走了水平拖动手势——导致点击进入大图后左右滑动切换作品完全不响应。
  现在未缩放时不挂载 InteractiveViewer（由外层 GestureDetector 接管滑动），
  缩放后才挂载以支持双指平移；缩放状态下滑动不触发作品切换（与设计合约
  一致）。

## 0.2.171（通知入口）

### Added

- 首页右上角新增通知铃铛（未读数字角标）：点击进入消息中心。未读数 = 服务端
  `isNew` 减去本地已读记录；登出或消息流加载失败时不显示角标。此前消息中心
  屏幕与本地已读存储已实现但没有 UI 入口，现兑现文档承诺的
  「通知（未读红点 + 本地已读）」。

## 0.2.170（登录页设置入口）

### Added

- 登录页右上角新增设置齿轮，点击进入应用设置（语言、外观、网络代理、日志与诊断、
  检查更新、关于）。这些恢复性页面不受登录路由限制，登录异常时仍可访问——兑现
  文档承诺的「登录失败时仍可使用设置」。

## 0.2.169（登录自愈与超时兜底）

### Fixed

- 登录页在获取到**已登录的网页会话**后自动关闭并返回应用，无需再手动点「完成」。
  覆盖「OAuth 已登录、仅网页会话丢失」的重新登录场景——旧逻辑只依赖 OAuth 状态
  转变，后者永远不触发，导致登录页卡住、其他页面无法访问。
- Cookie 保险库只在 OAuth 账号已登录且用户名匹配时恢复网页会话；登出状态下不再
  注入，避免登录页在 OAuth 登录完成前被自动关闭。
- 升级 dakit_api 0.1.30：官方 API 传输层补上**接收超时**（原配置只设了连接超时，
  dio 接收超时默认为空）——连接建立后服务端不响应（例如部分网络下
  api.deviantart.com 被挂起）会无限转圈，现在会明确报错而非永远加载。
- 新增 shell 导航回归测试（push/pop 与首启 splash 重定向两条路径）。

## 0.2.168（网页会话 Cookie 持久化）

### Fixed

- 更新 App 后「推荐」流消失并提示重新登录的问题。根因：登录 Cookie 只存在平台
  WebView 数据存储中，更新或系统清理后丢失；而自动刷新只能重建匿名会话，无法
  恢复需要登录身份的个性化推荐。
- 现在登录后会快照 deviantart.com 的 Cookie 到应用自己的存储；冷启动时若检测到
  WebView 中已无登录 Cookie（且保存的会话属于当前 OAuth 账号），会自动重新注入，
  「推荐」无需重新登录即可恢复。网页会话仍以 OAuth 为唯一身份，不构成二次登录。

## 0.2.167（更新提醒：展示更新内容）

### Added

- 更新横幅可点击：弹出「更新内容」对话框，展示该版本对应的发布说明
  （来自 GitHub Release 的正文，即 `RELEASE_NOTES.md` 对应章节），并提供
  下载按钮；设置页「检查更新」对话框同样展示更新说明。
- 更新检查失败不再写入节流时间戳：失败后下一次启动/回到前台立即重试，
  而不是等满一小时（`api.github.com` 在某些网络不可达时，原先会导致
  更新提示永远不出现）。

## 0.2.166（移除 Scraps）

### Removed

- 画师页的 Scraps 标签及对应的网页抓取器（`gallection/contents?scraps_folder=true`）、
  provider、文案与测试。官网画师导航本身不提供 Scraps 入口，且大部分画师不使用
  Scraps 文件夹，标签整体移除。

## 0.2.165（画师页标签顺序）

### Changed

- Scraps 标签从第二位移到最后一个位置（作品 / 收藏 / 文章 / 画集与收藏夹 /
  Scraps）——大部分画师不使用 Scraps，放在前面会让常用标签更难找。

## 0.2.164（Scraps 会话修复）

### Fixed

- Scraps 端点严格依赖配对的 Cookie + CSRF 会话：会话缺失或过期时，接口返回空列表
  （被误显示为「暂无 Scraps」）或 400 错误。现在会检测这种「会话形状的失败」，
  强制刷新匿名浏览器会话后重试一次（与 rfy 流的恢复契约一致），仍失败才给出
  可重试的错误提示，不再把会话问题伪装成空文件夹。
- Scraps 标签改为懒加载：只有打开该标签才发起请求，浏览画师页不再触发隐藏
  浏览器的会话刷新。
- 画集内搜索（`gallection/search`）同样加上会话失效后的刷新重试。

## 0.2.163（真实搜索与画师页增强）

### Added

- 搜索改为真实结果：官方 API 已移除搜索端点，App 改用网页端
  `_puppy/dabrowse/search/deviations`（复用 WebView 会话，含一次 CSRF 刷新重试），
  无网页会话时回退官方接口。
- 画师页新增 Scraps 标签：官方 `gallery/folders` 不含 Scraps，改读
  `gallection/contents?scraps_folder=true`（匿名 CSRF 即可）。
- 画师「作品」标签新增关键词搜索：`gallection/search` 在作者自己的画廊里
  搜索作品（官方接口无此能力）。
- 画师资料显示关注者数与加入时间：官方 `user/profile` 不提供，改读
  `dauserprofile/init/about`，字段缺失时自动省略。

## 0.2.162（会话恢复与搜索体验）

### Fixed

- 会话恢复改为以安全存储中令牌的实际存在为准，不再依赖可能过期的「会话证据」标志：
  网络/存储/超时错误在令牌存在时一律保留登录态，不再误显示为未登录。
- 标签排序修复：上游 `dakit_api` 0.1.27 将 `browse/tags` 的 `mode` 改为
  `newest`/`popular`（之前误用 `recent` 被忽略），「最新 / 热门」现在真正不同。

### Changed

- 搜索页：最近搜索移到最上方，改为紧凑的标签 chip（不再是大列表项）。
- 「为你推荐」标签新增持久化的浏览兴趣记录：打开作品时记录其标签，重启后
  推荐标签仍可用（收藏 3 > 关注 2 > 浏览 1 加权排序）。

## 0.2.161（推荐标签预览图）

### Changed

- 搜索页「为你推荐」标签从文字 chip 改为作品预览图卡片（与热门标签一致）。
  排序沿用兴趣加权（收藏 > 关注 > 浏览历史），最感兴趣的标签排最前。

## 0.2.160（更新提醒更灵敏 + 标签预览图）

### Changed

- 更新检查节流从 24 小时降到 1 小时，并在 App 回到前台时重新检查，新版本发布后
  下次打开/回到前台即可看到提示，不再被一整天节流压制。

### Added

- 搜索页「热门标签」改为带作品预览图的卡片（Pixiv 风格）：每个标签取该标签
  **最热门**的一件作品作为预览图（官方 `browse/tags` + `mode=popular` 取第一条），
  点击进入标签页。

## 0.2.159（标签与搜索排序）

### Added

- 标签页新增「最新 / 热门」排序：上游 `dakit_api` 0.1.26 的 `tag` 支持
  `BrowseSort`，通过官方 `browse/tags` 的 `mode` 参数按最新或最热排序。
- 搜索结果新增「最新优先」排序：官方搜索无服务端排序，改为客户端按发布时间
  对已加载结果排序。

## 0.2.158（作品卡片文字移到图下）

### Changed

- 作品卡片（信息流、关注动态、搜索、收藏集、相关推荐等所有网格）的标题和作者名从
  图片上的渐变遮罩移到图片下方独立的信息区，不再遮挡图片。

## 0.2.157（链接打开作品修复）

### Fixed

- 通过链接打开作品一直报「api endpoint not found」的根因：详情页直接用数字 ID 请求
  官方 `deviation/{id}` 接口，而官方接口只接受 UUID。现在先通过 `dadeviation/init`
  把数字 ID 解析成 UUID 再请求。
- 链接解析会保留作者名（`/artwork/<id>?username=<author>`），供数字 ID 解析使用
  （此前从链接进入时作者为空，R18 作品解析失败）。

## 0.2.156（R18 链接打开修复）

### Fixed

- 通过链接打开 R18 作品时提示「api endpoint not found」：数字 ID 解析接口
  `dadeviation/init` 未传 `mature_content`，R18 作品被返回 404。现在作品与日志
  的 init 请求都带上 `mature_content: true`。

## 0.2.155（关注动态更多用户）

### Changed

- 关注动态每页从 24 提升到 50 条，顶部头像条不再只被少数频繁更新的画师占据，
  能显示更多关注用户。

## 0.2.154（登录成功反馈）

### Added

- OAuth 成功瞬间在登录页显示全屏「登录成功 / 正在同步登录状态」覆盖层，同步完成后再
  关闭并回到首页。首次用户不再停留在看起来还在登录的网页上。

## 0.2.153（登录状态持久化修复）

### Fixed

- 升级 `dakit_flutter` 0.1.10：Android 安全存储默认的 `resetOnError` 会在任何解密失败时
  删除已存值，导致 OAuth 令牌在重启后丢失、被强制重新登录。现在令牌在瞬时
  Keystore/解密错误下会保留，下次启动重试读取。

## 0.2.152（登录页键盘流畅度）

### Changed

- 登录页 Scaffold 关闭 `resizeToAvoidBottomInset`：Android `adjustResize` 已让窗口随
  键盘收缩，Flutter 再收缩一次会让 WebView 发生第二次布局重排并掉帧。只保留一次收缩。

## 0.2.151（登录反馈）

### Added

- 全新登录（登出后重新登录）完成时显示「登录成功」提示。
- 登录失败时在登录页显示具体原因，避免「网页已登录但 OAuth 令牌未保存」被静默当作成功。

## 0.2.150（重启后会话恢复）

### Fixed

- 启动时恢复持久化的网页会话（Cookie/CSRF 状态）。此前只恢复了 OAuth，网页会话状态
  从未在启动时读取，导致重启后「推荐」一直显示登录引导，看起来像掉登陆。
- 推荐流请求失败时会先用持久化 Cookie 静默刷新 CSRF 并重试一次，避免会话过期后一直失败。

## 0.2.149（登录后黑屏与验证提示修复）

### Fixed

- 修复登录完成后黑屏：首次登录时登录页由启动重定向成为根路由，0.2.148 改为自行关闭后，
  直接 `pop` 会清空导航栈导致黑屏。现在有上级路由时返回原页面，否则回到首页。
- 登录进行中在顶部显示人机验证提示条，说明如出现验证请按页面提示完成。

## 0.2.148（登录后推荐不刷新修复）

### Fixed

- 移除 `/web-login` 的登录后路由重定向。此前 OAuth 一完成，路由就把登录页弹回首页，
  网页 Cookie/CSRF 会话还没来得及上报，「推荐」因此永远拿不到网页会话、一直显示登录引导，
  点击登录又被路由弹回首页，形成死循环。现在登录页在网页会话上报后自行关闭，并回到
  进入时的标签页（修复从「关注动态」登录后跳错页面的问题）。

## 0.2.147（登录页键盘卡顿修复）

### Fixed

- 移除登录 WebView 的透明背景：Android 上 `transparentBackground` 会强制软件合成，
  软键盘弹出引起表面尺寸变化时导致极端掉帧。改为不透明背景后恢复正常硬件加速。
- 加载进度条只在可见且有意义的进度变化时才重建，避免进度事件频繁触发整树重建。

## 0.2.146（Android 启动闪退修复）

### Fixed

- Android 包名改为 `app.daviewer` 后入口类 `MainActivity` 未同步迁移，导致启动
  `ClassNotFoundException` 直接闪退；现已迁移到 `app.daviewer` 包。

## 0.2.145（不打扰的更新提醒与隐私友好的问题报告）

### Added

- 首页新增轻量更新提醒：启动后每天最多静默检查一次 GitHub 最新版本，有新版本时显示
  可关闭的细条；被忽略的版本不会再次出现。失败完全静默，不弹窗、不自动下载。
- 「日志与诊断」页新增「报告问题」：在用户自己的浏览器中打开预填好的 GitHub Issue，
  只含版本号、系统平台，以及可选勾选的一段已脱敏日志（抹去 token、Cookie、邮箱）。
  全程零遥测，App 不会自动上传任何数据。
- 检测到上次运行有未捕获错误时，诊断页顶部给出轻声提示，可查看日志或报告问题。

### Changed

- 设置页的版本检查逻辑与首页更新提醒共用同一套版本比较与每日节流实现。

## 0.2.144（恢复个性化推荐流）

### Fixed

- 首页「推荐」重新回到网页版 `rfy/deviations` 个性化推荐流（使用网页 Cookie + CSRF），
  与官网推荐一致；此前被误改成官方通用 `browse/home` 发现流。
- 登录与网络设置页面移除残留的「系统浏览器」旧文案，改为内嵌网页登录的准确说明。

### Removed

- 删除已无调用者的系统浏览器登录相关文案与错误提示。

## 0.2.143（分享补全与登录态修复）

### Fixed

- 用户相关 Provider 同时监听登录状态与账号 id；退出、账号资料暂时加载失败或重新登录时，
  首页、关注、收藏和通知不再沿用前一账号缓存。
- 设置页「退出」等待退出完成后再提示，避免提示与状态不同步。
- 已登录用户误入登录页时直接回到首页，不再停留在登录页。
- 登录界面按失败类型给出可执行提示（安全存储、回调超时、网络失败等），
  “Unable to access” 等内部文本只进入诊断日志。

### Added

- 作者主页、画廊分组、收藏集内容和标签页的顶部补全原生分享入口；作品详情分享改用
  系统原生分享面板。

## 0.2.142（中性包名与单网页登录）

### Changed

- Android 包名与 macOS Bundle ID 改为中性的 `app.daviewer`，不再包含开发者 GitHub
  用户名。破坏性变更：需卸载旧版后重新安装并重新登录。
- 移除独立的“社交登录”入口：登录只有一个内嵌网页入口，官方登录页里的 Google、
  Apple 等入口都能直接使用；一次登录同时建立 OAuth 与网页会话。

## 0.2.141（原生分享与发现流命名）

### Added

- 作品、作者主页、画廊分组、收藏集和标签支持系统原生分享；分享的是公开 DeviantArt
  canonical URL，不包含 OAuth token、媒体签名参数或本机路径。
- 原生分享桥不可用时自动复制公开链接并明确提示，避免按钮无响应。

### Changed

- 首页首个标签从「推荐 / For you」更正为「发现 / Discover」。官方 OAuth
  `browse/home` 是通用发现流，并不等价于网页版 Cookie/CSRF 驱动的个性化
  `rfy/deviations`；文档和 UI 不再作错误承诺。

## 0.2.139（登录恢复存储解耦）

### Fixed

- 登录回到 App 内嵌网页：一个登录入口、一次登录同时建立 OAuth 与网页会话，
  不再跳转系统浏览器；网页 Cookie/CSRF 会话恢复，个性化推荐与收藏集适配器重新可用。
- 修复 OAuth 回调、code 校验与 token 换取均已成功后，macOS Keychain 错误
  `-34018` 仍让 pending PKCE 清理抛出 “Unable to access”，并把成功登录回滚成失败。
- DAKit 0.1.25 将 pending PKCE 安全存储明确降为冷启动恢复能力：同进程授权持有
  内存中的 verifier；记录写入失败不阻断登录，成功、取消、超时后的清理失败只记诊断，
  不遮蔽原始结果。
- DAViewer 的 macOS token 与 pending 存储从 `DAViewer OAuth` 迁到 `DAViewer Account`，
  不再查询旧签名留下的不可访问项目。token 写入仍是登录的硬提交点。
- 本地退出现在先写入持久化退出标记，再尝试撤销 token；重启不会读取残留 token
  并把用户自动登录回来。退出同时取消正在等待的 OAuth 回调。

## 0.2.138（单一登录与官方推荐闭环）

### Fixed

- 登录页收敛为一个「登录或注册」入口：只创建一次 OAuth/PKCE 事务，
  由系统浏览器中的 DeviantArt 官方页提供账号、注册、Google、Apple 与 Facebook。
- 首页「推荐」改用官方 OAuth `browse/home`，不再依赖另一份网页 Cookie 身份；
  修复 Google 登录回调成功后首页还要求再登录一次。
- 移除内嵌密码 WebView、模拟点击社交按钮和 DOM 人机验证猜测；403/429/503
  安全页由真实浏览器呈现，App 不再把它错报为断网。
- 重新打开授权会复用同一个 authorize URI 和 PKCE state；取消会完整清理待处理
  事务，避免旧回调占用下一次登录。
- 未登录首次启动只进入原生登录引导，不启动个性化网页会话；作品详情所需
  的网站数据改为按需匿名刷新，失败时只重试或降级，不弹出第二次登录。
- 修复 DeviantArt 将失效 refresh token 返回为非标准 `invalid_request` 时，客户端将它
  误当临时断网并保留假登录态的问题。DAKit 现在统一映射为 `oauth.refresh.invalid`，
  DAViewer 也兼容旧错误形状并清理无法恢复的令牌，不再让首页无限重试。
- 修复 macOS Keychain 拒绝删除旧令牌时，DAKit 的内部 logout 状态无法复位、
  导致之后每次官方授权虽已换到新 token 却仍被取消的问题；存储清理失败现在
  也一定退出 logout 状态，客户端不再对同一失效通知重复注销。
- OAuth 成功的提交点改为「新 token 已保存」；macOS Keychain 无法清理已过期的
  pending PKCE 记录时只记录警告，不再把已成功的首次授权推翻成登录失败。
- 普通冷启动没有 OAuth 回调时不再读取 pending PKCE 安全存储，避免 macOS
  Keychain 暂时不可用时让启动长时间停在「正在建立官方授权流程」。

### Changed

- 连通性测试只承诺 App 内 API、媒体和下载路径；官方登录页明确遵循系统
  浏览器的系统代理或 VPN，不再声称 App 手动代理可以接管外部浏览器。
- 日志页面将目录显示为 `DAViewer/logs`，日志初始化不再输出物理沙箱路径。
  Android/macOS 应用标识保持不变，避免升级被系统当成新 App 而丢失下载与设置。
- macOS Release 改用 CI 私有的稳定自签预览身份及新的 `DAViewer OAuth` 钥匙串项目。
  它仍不是 Apple Developer ID、仍未公证，但后续更新不再因 ad-hoc cdhash 改变而要求
  Mac 登录密码。为避免旧项目触发可疑密码框，从旧版升级需重新完成一次官方登录；
  其他本地数据不受影响。
- 删除已无调用者的网页个性化推荐请求器，保留公开作品载荷的容错映射；
  重写登录、代理、架构、网页适配器及中英文用户文档。

## 0.2.137（真实登录分流与系统浏览器回调）

### Fixed

- 删除 Google / Apple 原生按钮“打开同一 WebView 后模拟点击网页控件”的伪分流；Google
  官方禁止嵌入式 user-agent，这条链路会产生白屏或 `disallowed_useragent`，无法靠修改
  User-Agent 稳定修复。
- 社交登录改为系统浏览器 OAuth/PKCE：Google、Apple、Facebook 以及官方页以后新增的
  提供商都由 DeviantArt 当前页面呈现，完成后通过一次性回调返回 App，不再要求第二次登录。
- Windows ZIP 版现在按当前用户注册 `dakit://oauth/callback`，并把浏览器启动的第二个进程
  安全转发给已运行实例；移动安装路径或升级后会自动修复注册路径。
- 社交登录成功但没有内嵌网页 Cookie 时，首页自动进入 OAuth 可用的“每日推荐”，不再把
  成功登录显示成网页会话过期或首页错误；关注、收藏、下载等功能立即可用。

### Changed

- 登录 UI 只保留两条真实链路：“社交账号（系统浏览器）”与“DeviantArt 账号（App 内）”。
  前者遵循系统代理/VPN，后者遵循 App 当前代理并同时建立个性化网页推荐会话。
- App 会通过当前网络只读探测 DeviantArt 官方登录页实际展示的社交提供商，仅用于动态
  更新按钮文案；探测失败退化为通用“社交账号”入口，不影响授权。
- 系统浏览器授权增加等待状态、重新打开同一个授权地址和明确取消；取消会清理 pending
  PKCE 事务，下一次登录不会复用旧 state。
- 代理 UI 明确提示 App 专用代理不能控制外部浏览器；国内用户使用社交登录时还需保证
  系统代理或 VPN 可用。更新中英文 README、登录、网络和架构文档。

## 0.2.136（会话证据与首次启动隔离）

### Fixed

- 修复首次安装、从未登录的用户在网络、上游或钥匙串检查失败时，被错误标记成“已登录”
  并进入首页后看到推荐接口错误的问题。
- 保留老用户的离线恢复：只要安全存储曾成功读写 OAuth token，非敏感会话证据就会记录；
  之后即使刷新 token 时断网，也不会清除登录态或要求重新登录。
- 明确注销会同时清除会话证据；普通网络失败、上游限流和验证页不会删除 token、Cookie
  或该证据。
- 修复 Linux 在 GNOME 代理模式已关闭时仍读取残留 host、误连失效代理的问题；手动模式
  现在优先 HTTPS 代理并回退 HTTP，PAC 自动模式会明确回退环境变量或直连。

### Changed

- 冷启动恢复改为双重判定：“错误是否可恢复”与“本机是否确有成功 OAuth 会话”必须同时
  成立才保留已登录状态。首次用户进入登录/代理/设置界面，老用户进入可离线恢复状态，
  不再用同一条模糊兜底处理。

## 0.2.135（登录验证状态机）

### Fixed

- 修复 Cloudflare、PerimeterX、reCAPTCHA、hCaptcha 或 Arkose 验证控件由脚本延迟插入、
  页面不触发 `loadStop` 时，App 看得到验证页却没有任何提示的问题。
- 修复验证页内部导航一开始就清除提示，以及 Google / Apple 弹窗关闭后仍轮询已经销毁的
  WebView 的问题。
- 403、429、503 先进入“上游安全验证正在载入”状态；只有持续确认不存在可交互验证页后，
  才会显示网络/代理故障，避免把人机验证误判成断网。

### Changed

- 主登录页和社交登录弹窗在授权期间持续侦测动态验证 iframe、DOM 控件、标题和 URL；
  验证完成后连续确认页面恢复正常，提示才自动消退。
- 新增常驻状态条、八秒即时提示、读屏实时播报和手动刷新入口；验证页继续保留 Cookie、
  DOM Storage、第三方 Cookie 与原 OAuth/PKCE 事务。

## 0.2.134（登录事务可重试热修复）

### Fixed

- 修复第一次登录白屏后返回，旧 OAuth/PKCE 事务仍等待回调十分钟，导致之后所有登录都
  被错误合并进旧事务并永久显示“正在建立官方授权流程”的严重问题。
- 返回登录方式页、关闭登录、重试、路由销毁或授权页 20 秒仍未开始导航时，现在都会
  取消并等待旧事务完整退出；下一次登录会生成全新的 PKCE state 与授权地址。
- 清除被取消事务遗留的待加载授权 URL，避免重建 WebView 后加载已经失效的旧地址。

### Changed

- 删除登录前无效且缺少 PKCE `code_challenge` 的 client-id 预检请求；真实的完整 PKCE
  授权 URL 现在立即进入 WebView，不再被额外代理请求阻塞成白屏。
- OAuth 取消被视为正常用户操作，不再显示为登录错误；新增协调器级回归测试，验证取消
  后可以立即创建 state 不同的新事务。

## 0.2.133（横幅预览与文字层）

### Fixed

- 修复移动端两列瀑布流中，横幅作品卡片过矮、固定的两行标题与作者层遮挡大部分画面的
  问题；极宽图片现在保留更多预览高度，中心主体更清晰。

### Changed

- 作品卡片改为响应式元数据布局：矮横幅使用单行标题与淡化的小号作者名，并移除作者
  图标；竖图和高卡片保留两行标题与完整作者信息。
- 首页、关注动态、搜索/标签、作者画廊、收藏夹、更多类似作品等共用同一横幅比例与
  文字层规则，桌面端保持原有宽高比上限。
- 架构文档补充作品预览卡片的移动端展示约束与回归测试要求。

## 0.2.132（登录人机验证识别）

### Fixed

- 修复使用代理登录时，DeviantArt 人机验证响应被误报为“登录页无法连接”的问题；
  App 会保留可交互验证页，完成验证后自动继续原登录流程。
- 主登录页与 Google / Apple 登录弹窗统一识别常见的 403、429、503 验证响应，
  不再关闭弹窗、显示代理故障或诱导用户无意义地重试。

### Changed

- 人机验证出现时显示独立的中英文说明，明确它不是网络故障；只有真实的 DNS、超时或
  无验证页面的 HTTP 失败才进入网络/代理排障。
- 网络与登录文档补充人机验证和维护者回归检查规则。

## 0.2.131（单次登录与社交授权）

### Fixed

- Google / Apple 登录改为从一开始进入同一个 App OAuth/PKCE 事务，不再先建立网页登录态、
  然后另起一次授权；修复首次 Google 登录白屏、必须第二次登录才成功的问题。
- 社交登录弹窗不再在 Cookie 刚出现时立即强制关闭并跳首页；优先等待官方
  `window.close`/回传，必要时恢复原授权地址，不会丢弃第一次授权状态。
- macOS 密码登录遇到已提交 Cookie 的临时 403 时恢复同一个 OAuth 地址，而不是跳首页
  中断授权。

### Changed

- 原生入口拆分为 DeviantArt、Google、Apple 三个真实动作：Google / Apple 会直接触发
  官方页面对应按钮，账号入口保留用户名密码表单，不再只是两个按钮打开同一网页。
- 社交提供商回传期间显示明确的“正在完成登录并继续授权”界面，取代空白 WebView；取消、
  网络失败和会话未提交分别保留可恢复提示与诊断日志。
- 重写中英文登录与会话文档，明确网页 Cookie/CSRF 与 App OAuth 仍是两种凭据，但首次
  登录由同一官方授权导航一次建立。

## 0.2.130（移动端网络引导与国际化）

### Fixed

- Android 原生读取系统 HTTP 代理，API、图片和下载不再因 Dart 网络层误判为直连而绕过
  手机系统代理；VPN 网络仍由系统正常接管。
- 修复代理地址示例像是在要求所有用户使用固定 `7892` 端口的问题；端口现在明确要求
  以用户自己的代理 App 所显示的 HTTP/Mixed 端口为准。
- 移动端明确区分“代理运行在本手机”和“代理运行在电脑/路由器”：前者可用
  `127.0.0.1`，后者必须填写局域网 IP 并开启“允许局域网”。

### Changed

- 登录入口会自动进行一次非阻塞连通性测试：直连可用时明确显示“无需代理”，只有连接
  不可用时才提示设置代理，不再把国际直连用户和受限网络用户套入同一种引导。
- 重做窄屏代理设置页：单列大按钮、端口来源说明、关闭键盘即可提交、“保存并测试”一
  步完成，并用成功/失败状态卡给出下一步；修改后返回登录页会自动复测。
- README 与网络文档补充端口占位符、手机 localhost、跨设备局域网代理和国内/国际网络
  的差异化处理原则。

## 0.2.129（原生登录入口与全链路代理）

### Added

- 登录不再打开即跳网页：新增原生登录入口，明确提供 DeviantArt 账号、Google / Apple、
  注册、找回密码，以及登录前的代理设置和连通性测试；只有用户点击后才打开官方页面。
- 网络代理页新增有效来源、DeviantArt 实时连通性测试和操作建议；支持
  `host:port` 与 `http://host:port` 两种输入。

### Fixed

- 修复 App 内手动代理只对部分 API 生效、不覆盖登录与后台 WebView 的问题；现在 API、
  图片、下载、网页登录和会话刷新共享同一有效网络路径。
- Google / Apple 登录的新窗口会作为 App 内第二层 WebView 承接；即使服务商先创建
  `about:blank` 再跳转也不会丢失窗口、Cookie 或登录结果。
- 修复手动代理重启即丢失，以及清除手动代理后仍残留旧地址、没有立刻重新检测系统代理
  的问题。
- Android 使用进程级 WebView 代理；Windows 的登录、后台刷新与 Cookie 读取共用同一个
  WebView2 代理环境；macOS 14+ 通过系统 WebKit API 应用 App 代理。
- 修复 Windows 注册表按协议保存 `http=...;https=...` 时被错误解析成无效主机的问题。
- WebView 代理切换改为串行执行，避免设置保存、后台刷新与重试同时发生时重复销毁或创建
  浏览器环境。

### Changed

- macOS 12/13 无法向系统 WebView 注入 App 内代理时会明确提示使用系统代理，而不是让
  用户在网页登录白屏和 API 成功之间猜测。
- 新增独立的网络与代理文档，明确自动选择优先级、平台覆盖、`all_proxy` 的桌面启动
  边界和发布前验证清单。

## 0.2.128（登录与会话恢复热修复）

### Fixed

- 修复 macOS 钥匙串项目改名后没有迁移旧 OAuth 令牌、导致升级后看起来像“数据丢失”
  并被强制重新登录的严重回归；现在双读新旧位置并无损复制，主动退出才同时清理。
- 冷启动的网络、DeviantArt 服务、超时或钥匙串临时错误不再被当成已登出；只有令牌
  确实不存在、被撤销或用户主动退出时才进入登录流程。
- 后台 WebView 的匿名、反爬或半加载页面不再覆盖有效网页登录态；只有同时读到 CSRF
  和用户名才允许刷新会话。
- 未登录时不再把设置及其网络代理、日志诊断、检查更新、关于页面重定向回首页；登录页
  新增常驻设置入口。
- 移动端登录支持 Google / Apple 新窗口跳转与第三方 Cookie；macOS 密码提交后的 403
  会等待登录 Cookie 落盘后恢复，不再只读取一次就失败。
- 首页首屏刷新改为单飞合并，生命周期、CSRF 与手动刷新不会并发覆盖；失败后停止自动
  重试并显示可执行的下拉刷新和代理设置说明，不再暴露内部错误。

### Changed

- 作者页标签明确区分“作品 / 收藏作品 / 文章 / 画集与收藏夹”，分类页进一步说明画集
  是作者自己的作品分类，收藏夹是作者收藏的他人作品。
- 重写登录与会话恢复文档，明确成人内容由 DeviantArt 账号设置控制，以及 macOS 包为
  未签名、未公证测试版。

## 0.2.122（收藏集封面与推荐优化）

### Fixed

- 收藏集卡片现在会显示封面图，不再是一片空白。
- 修复「已被收录」和「建议收藏集」重复显示同一收藏集的问题。
- 纯文字作品不再在「更多类似作品」里显示成空白卡片。

## 0.2.121（作品发现 · 设置与体验）

### Added

- 详情页新增「已被收录 / 建议收藏集」，点开即可浏览完整收藏集。
- 新增「作者更多作品」和「相似画师」两个推荐区块。
- 设置新增深色模式、清除缓存、检查更新；语言和外观会记住你的选择。
- 通知支持一键「全部已读」。
- 搜索改为边输入边出结果。

### Fixed

- 修复部分作品下收藏集不显示的问题。
- 修复左右滑动预览相邻作品时的偶发异常。
- 修复开发版版本号被误判为有新版本的问题。
- 修复 Android 下载后缩略图不显示的问题。

### Changed

- 主题与动画更现代（页面过渡、底部弹层、进度指示、提示气泡）。
- 首页改用瀑布流布局并保持标签页滚动位置；作品卡新增 GIF / 多图角标。
- 时间戳自动刷新；界面间距与角标文案更统一。

## 0.2.97（关注空状态与网页推荐新格式）

### Fixed

- 修复未关注任何用户时，DeviantArt 官方端点返回空对象、却被通用分页解析器当成
  `results` 缺失错误并直接展示英文内部信息的问题。上游 `dakit_api 0.1.18` 现在把
  该响应识别为正常空关注流；矛盾的分页字段仍会保留为可诊断异常。
- 修复 DeviantArt 把相关推荐迁移到流式 `window.__RCACHE__.relatedContent` 后，客户
  端仍只读取旧 `window.__INITIAL_STATE__`，导致网页有类似作品而 App 显示为空的
  问题。新旧页面格式现已同时兼容，并通过真实作品页快照验证。

### Changed

- 空关注流改为明确说明“尚未关注任何用户”，并提供“发现并关注”入口。
- 相关推荐加载、空结果和恢复失败文案不再暴露“官方 API”“备用源”“页面结构”等
  内部实现；原始错误仍写入诊断日志，用户只看到可执行的检查更新或稍后重试提示。

## 0.2.96（类似作品刷新语义）

### Fixed

- 修复网页推荐初始 metadata 尚未注入或解析失败、而 OAuth 备用源恰好为空时，被错误
  判定成“确实没有类似作品”的问题；这种不确定状态现在会自动恢复一次，仍失败才显示
  对应的页面结构、网络、会话或服务原因。
- 修复刷新只触发 Provider 失效、却没有等待新请求完成的问题；刷新进度和最终结果现在
  属于同一次操作，不再出现圆圈结束后界面毫无说明。

### Changed

- 已有类似作品在检查更新时保持可见，只在标题旁显示小型进度，不再用整块圆圈覆盖
  内容。完成后明确提示“已更新”“没有变化”或“重新检查后仍为空”。
- 真实空结果会说明网页与备用来源均未返回可显示内容，并提供语义明确的“检查更新”，
  不再使用让用户误以为软件必然故障的“重试”。

## 0.2.95（关注流标签根因修复）

### Fixed

- 修正 0.2.94 的错误数据假设：`deviation/{id}` 并不可靠返回标签，关注动态的精简
  作品现在改从官方 `deviation/metadata` 端点补全标签，因此不再出现“测试通过但真实
  详情仍无标签”。
- 标签补全失败仍保持详情页可用且允许下次重试；官方确认无标签的作品会被缓存，避免
  每次进入详情都重复请求。

### Changed

- 上游 `dakit_api 0.1.17` 新增 `OfficialArtworkMetadataRepository.tags`，DaViewer
  直接复用 SDK 能力，不再在 App 中维护重复的端点解析逻辑。

## 0.2.94（关注流标签与移动端缩放）

### Fixed

- 修复从「关注动态」打开作品时标签为空：关注流返回的是省略标签的精简作品对象，
  详情页现在仅在需要时通过规范作品接口补全，并把结果写回统一缓存。
- 修复作品双击放大后无法拖动：放大状态会完整关闭外层作品切换手势识别器，让图片
  查看器独占横向与纵向平移；1× 时仍可左右切换作品，多图边缘规则保持不变。
- 修复后续列表刷新可能再次用精简对象覆盖已补全标签的问题；缓存合并现在只接受
  数据增量，不允许详情字段降级。确实没有标签的作品也会记录“已确认”，避免重复请求。

### Changed

- App 版本显示改为直接读取 Flutter 构建版本，删除设置页内重复维护的硬编码版本号。
- Release 说明改为读取当前版本的 CHANGELOG 章节，并保留历史 Release/标签；缺少章节
  时才回退到提交摘要。
- 重写中英文数据边界、代理、测试和发布文档，补充稀疏数据合并与缩放手势所有权约束。
- macOS 产物明确改名为 `macos-unsigned-preview`，安装说明与 Release 顶部同步提示其
  未使用 Apple Developer ID、未公证，以及系统钥匙串可能要求 Mac 登录密码。

### Removed

- 删除与应用图标完全重复的 `docs/icon.png`，README 直接复用构建资产，避免两份 Logo
  漂移。

## 0.2.93（连续浏览与相关推荐状态）

### Fixed

- 修复详情路由复用同一个页面状态，导致第一次切换作品后导航锁未释放、无法继续左右
  滑动的问题；每个作品现在拥有独立页面键，并保留同一浏览序列，可连续切换到底。
- 作品切换不再闪现：拖动时页面提供跟手位移反馈，松手后按前后方向执行 280ms
  滑动淡入过渡；顶部前后按钮与多图边缘切换使用同一动画链路。
- 「更多类似作品」的正常空结果不再显示“重试”，避免把“网站没有相关推荐”误导为
  软件或网络故障。

### Changed

- 相关推荐网络或会话失败会先自动恢复一次；仍失败时分别说明网络/代理、登录会话、
  DeviantArt 服务限制、网页结构变化或未知异常。会话异常直接提供登录入口，其它异常
  使用含义明确的“重新加载”。原始双源错误只写入诊断日志。
- 审计 DAKit 上游后继续复用其 `DAKitFailureKind` 与 `retryable` 契约；官方 API 错误
  分类已经完整，本次仅在 DAViewer 组合网页源与官方源，并实现应用级恢复和交互，未
  引入重复的 SDK 模型或发布无必要的上游版本。

## 0.2.92（紧凑标签 · 连续浏览）

### Added

- 从首页、搜索、标签、画廊、画集、收藏、关注流和 More Like This 打开作品时，会把
  当前可见作品序列带入详情；左右滑动或顶部前后按钮可连续浏览，切换使用路由替换，
  返回时仍回到原列表而不会堆积详情页历史。
- 单图全屏在 1× 缩放时支持左右切作品；放大后横向手势只平移图片。多图作品优先在
  内部翻页，仅在首图/末图继续越界滑动超过阈值时切换作品，避免两种手势互相抢占。

### Changed

- 详情、搜索和标签页的标签统一为 34px 单行横向条，不再因标签数量换行占满屏幕。
- 标签作品页的相关标签向下滚动时自动折叠、回到顶部或向上滚动时恢复。
- README 中删除重复的手动发布步骤和已经过时的 More Like This 数据流说明，重新明确
  DAKit（OAuth/官方 API/模型/传输）与 DAViewer（网页源/原生交互）的边界。

## 0.2.91（网页同源相关推荐）

### Fixed

- 「更多类似作品」优先读取作品网页当前实际展示的 `gallery` / `recommended`
  数据，不再把旧 OAuth 预览接口的空结果误判为“网站也没有推荐”；官方接口保留为
  网页源失败时的兜底。
- 网页推荐中的数字作品 ID、规范化作者和媒体实体会还原为原生作品模型并写入详情
  缓存，卡片有真实预览且点按继续进入 App 内作品详情；推广位不会混入相关推荐。
- 新增真实页面快照回归入口，已用 `Comm-New-Jersey-1371131855` 验证可还原网页
  推荐，避免以后只验证接口形状却漏掉实际网站行为。

### Changed

- 网络层新增 `all_proxy` / `ALL_PROXY`、`HTTP_PROXY`、`HTTPS_PROXY` 环境变量支持；
  App 内手动代理与系统代理仍保持更高优先级。
- 相关推荐日志会分别记录网页源和 OAuth 源的结果数量与错误，便于区分网页结构变化、
  会话问题和官方接口空结果。

## 0.2.90（下载权限与失败原因）

### Fixed

- 下载按钮不再直接信任推荐流中的缓存 URL；数字作品 ID 会先解析为 UUID，再通过官方
  原图端点实时确认当前账号的下载权限，避免额度、订阅或作者设置变化后按钮仍错误点亮。
- 受限视频不再把海报图片误判为可下载的视频；图片作品需要降级时会选择当前展示资源
  中分辨率最高的一张，并明确说明保存的是预览图而非原图。
- 下载权限探测的临时网络 / 会话故障不再伪装成永久不可下载，现显示“无法确认权限”
  及重新检查入口。

### Changed

- 不可下载时在按钮旁持续显示登录、购买、作者关闭、免费次数用尽、作品删除等具体
  原因；未知的 DeviantArt 原因也会保留，不再被统一文案覆盖。
- 后台下载的 401/403、404、网络超时及本地存储失败会在详情页即时提示，并在下载
  列表中持久显示原因；失败任务可直接重试。

## 0.2.89（相关推荐 · 登录 · 媒体体验）

### Fixed

- 「更多类似作品」适配官方当前的稀疏预览契约：缺少标题、作者或媒体的合法条目会
  通过作品详情接口分批补全，不再被映射器全部丢弃；真实空结果也会显示说明和重试，
  不再静默隐藏。
- macOS 首次登录改为先完整提交网页登录 Cookie/CSRF，再进入 OAuth 授权，消除密码
  表单与授权页抢导航造成的 403；若密码已接受且 Cookie 已提交，短暂 403 会自动回到
  首页刷新会话。
- 视频初始化失败不再触发未初始化控制器异常，现提供原生失败态和重试；切到后台会
  自动暂停，并优先选择可用的最高画质流。

### Changed

- 下载预览与作品详情大图改用同一个全屏查看器，统一双击定位放大、捏合缩放、缩放
  范围与重置按钮。
- GIF 使用带进度的磁盘缓存、按屏幕尺寸解码并可点开缩放，减少重复下载和大图内存
  峰值。
- 富文本内嵌图片改用磁盘缓存、加载进度、尺寸占位和失败重试，并兼容协议相对及站内
  相对图片地址。

## 0.2.88（macOS 启动修复）

### Fixed

- 首次安装且尚未登录时直接进入统一登录页，不再先初始化个性化推荐并把“未登录”
  当成推荐加载错误；恢复网页会话也提前到 OAuth 路由切换之前完成。
- 推荐流只有在网页身份与 CSRF 均就绪后才会创建请求；登录失败会在登录页显示明确
  原因和可重试操作，不再留下无法继续的网页。
- 移除 macOS Release 中无效的空 `keychain-access-groups` 权限。该受限权限无法由
  临时签名授权，会导致 launchd 在应用进入 `main()` 前直接终止进程。
- Release 构建关闭开发权限自动注入，并在打包前重新应用最小生产权限。

### Changed

- macOS CI 新增包内签名、Intel/Apple Silicon 双架构校验，以及真实启动 8 秒的
  smoke test，避免再次把“编译成功”误判为“客户端可以打开”。
- 安装文档明确区分临时签名与 Apple Developer ID 公证，并补充安全的首次运行步骤。

## 0.2.87（下载可靠性 · 类似作品自愈）

### Fixed

- 下载列表改为直接合并后台任务快照，不再在每个进度事件上重读整个原生数据库；
  同时释放页面控制器的流监听，消除高频刷新竞态与长期监听泄漏。
- 批量删除期间禁用重复点击并显示进度；任一文件删除失败时重新同步列表、保留失败
  记录并显示错误，用户可以重试，不再把部分失败误报成成功。
- 「更多类似作品」不再在错误时静默消失：增加原生失败态与重试按钮。数字作品 ID
  重试会等待网页 CSRF 会话刷新真正完成，再重新执行 UUID 解析。
- 配合 DAKit API 的逐条容错：单个已删除、受限或结构异常的推荐不会拖垮同批其它
  可用作品。

### Changed

- 升级到 `dakit_core` 0.1.11、`dakit_api` 0.1.15、`dakit_flutter` 0.1.8；
  删除契约现明确为破坏性操作，并在本地文件删除失败时保留记录与路径元数据。

## 0.2.85（回退收藏集区块）

### Fixed

- 下载页删除已结束任务前新增二次确认，并明确告知会同时删除
  下载记录和本地文件，避免误触造成不可恢复的文件丢失。

### Removed

- 移除作品详情页的「已被收录 / 建议收藏集」两个区块：当前没有收藏集原生页面，
  点击只能跳浏览器、且无预览图，体验不完整。待实现收藏集原生页后再补回
  （DAKit 的 `MoreLikeThisResult` 数据映射保留，未来可直接复用）。

## 0.2.84（新应用图标 · 文案调整）

### Changed

- 更新应用启动图标与项目 Logo（Android / macOS / Windows 各平台 + README）
- 调整 README 简介文案，改为更中性的表述

## 0.2.83（详情页收藏集区块）

### Added

- 作品详情页在「更多类似作品」下方新增两个横向卡片区块：「已被收录」与
  「建议收藏集」。两者与 More Like This 共用同一次 `browse/morelikethis/preview`
  请求，空则自动隐藏；卡片展示收藏集封面 + 名称 + 作者，点按打开收藏集作者主页
  （`dakit_api` 0.1.13：收藏集分组在接口形状漂移时降级为空，不拖垮相关作品）。

## 0.2.82（为 Suggested Collections 预留数据）

### Changed

- More Like This 的数据层升级为完整的 `MoreLikeThisResult`（相关作品 +
  `featured_in_collections` + `suggested_collections`），一次请求即可为将来详情页的
  收藏集区块提供数据，无需再改 DAKit 传输层（`dakit_core` 0.1.10 /
  `dakit_api` 0.1.12）。当前界面仍只展示相关作品瀑布流。

## 0.2.81（More Like This 修复）

### Fixed

- **「更多类似作品」空白**：作品详情页底部的 More Like This 之前请求的是已下线的
  `browse/morelikethis` 分页接口，导致静默失败、一片空白。现已改走官方的
  `browse/morelikethis/preview` 接口，并把 `more_from_da` + `more_from_artist`
  去重合并展示（`dakit_api` 0.1.11 / `dakit_core` 0.1.9）。

## 0.2.80（macOS 钥匙串体验）

### Fixed

- **macOS「钥匙串」弹窗**：登录后 macOS 可能弹出「DAViewer 想要使用钥匙串中的机密信息」。
  现在钥匙串条目命名为「DAViewer」（不再显示 `flutter_secure_storage_service` 这种
  让人误以为在读取任意机密的实现细节），并为沙盒应用补齐 `keychain-access-groups`
  授权（`dakit_flutter` 0.1.7 新增 `clientSecureStorage(serviceName:)`）。应用依旧只在
  钥匙串保存自己登录 DeviantArt 后获得的 OAuth 令牌，不碰其它机密。

## 0.2.67 – 0.2.74（瀑布流 · 付费内容 · 个性化搜索 · 细节打磨）

### Added

- **瀑布流**：作品网格改为按图片真实比例渲染的 Masonry 布局（DAKit 新增
  `MasonryGridView` / `SliverMasonryGrid`）
- **付费/订阅内容优雅降级**：付费作品回退显示可访问的缩略图；完全无可用资源时
  显示「🔒 需要购买/受限」占位，而非裂图
- **个性化推荐标签**：搜索页「为你推荐」结合收藏（权重 3）、关注画师（2）、
  浏览历史（1）加权生成
- **更多类似作品**：详情页底部展示与当前作品相关的作品，以瀑布流网格呈现
  （官方 `browse/morelikethis` 接口）

### Changed

- **搜索页重构**：搜索历史抽成独立 Provider；拆分标签 chips / 历史列表组件；
  用户搜索结果头像改用缓存 + 降采样
- 登录帮助文案规范化
- 详情页作者行本地化（`by` → 中文「作者：」）

### Fixed

- 修复下载按钮在不可下载作品上仍可点击但无反应的问题（仅回退到真正可下载的资源）
- 修复 Android 首次打开首页「推荐」先出内容又闪一遍的问题（CSRF 刷新改为静默）
- 修复删除下载只删记录不删文件（`dakit_flutter` 0.1.6：删除时连同文件一起删）
- 下载页「打开文件夹」仅桌面端显示（移动端无对应能力）
- 区分「暂无图片」与「图片加载失败」，避免用户误判为网络问题

## 0.2.43 – 0.2.66（关注动态一级入口 · 现代化 UI · 性能与内存）

### Added

- **关注动态**提升为底部一级标签（B站「动态」式），顶部显示按更新时间排序的
  关注画师头像排（含相对更新时间），点头像直达画师主页
- 登录成功提醒（网页登录成功 / OAuth 登录成功）；登录页「?」帮助面板 +
  找回密码 / 注册 DeviantArt 账号快捷入口（浏览器打开）
- 文章（journal）解析补全：`da-gif` 动图 / `da-video` 视频 / `@提及` /
  `image` 图片 / 分割线 / 文学嵌入；嵌入图片改为可点链接，正文链接可在浏览器打开
- 作品卡片长按菜单：查看详情 / 收藏·取消收藏 / 复制链接 / 在浏览器打开
- 下载页「打开文件夹」（系统文件管理器定位）与早期的记录清理功能（后续版本已改为
  明确删除记录与本地文件，并要求二次确认）
- 通知页未读区分（主题色圆点 + 加粗）；未登录时显示登录引导
- 搜索历史单条删除

### Changed

- **UI 现代化**：全 App 骨架屏加载（微光）；作品卡片满幅图 + 底部渐变标题/作者；
  网格→详情 Hero 过渡；详情红心弹跳 + 内容淡入；设置页分组圆角卡片；
  统一下拉刷新样式；空态/错误态重设计 + 「去发现」引导
- **性能与内存**：全局图片缓存上限（200 张 / 64MB）；缩略图 480px / 头像 120px /
  详情 1080px 降采样；详情与全屏渐进式加载（糊→清）；feed 常驻缓存避免切页重载；
  冷启动账户信息后台加载 + 网络超时兜底
- 依赖 `dakit_core` 升级到 0.1.6、`dakit_flutter` 0.1.4（新增 `TransferManager.remove`）

### Fixed

- 修复 Android 冷启动可能卡在启动页、首页反复加载的问题（OAuth 网络调用超时兜底）
- 修复 journal 内嵌图片 / GIF / 提及等节点被丢弃的问题
- 修复 journal 多页漫画（如 31 页）加载内存过大的问题（改用 preview 尺寸）

## 0.2.5 – 0.2.42（原生推荐流 · 富文本详情 · 完整作者页）

### Added

- **个性化推荐流**：首页「推荐」改为原生界面，数据来自网页版个性化接口
  （`rfy/deviations`），与官网推荐一致；「每日推荐」「关注」两个标签也全部原生渲染
- **登录态同步提示条**：网页会话与 OAuth 会话不一致时，首页顶部一键补全
- **冷启动会话恢复**：静默重建网页会话（Cookie + CSRF），无需每次手动登录
- **多图作品**：左右翻页 + 页码浮层 + 圆点指示器；作品卡片显示多图角标
- **标签**：详情页显示 `#标签`，点击进入标签作品流；标签页带「相关标签」推荐
- **搜索**：搜索用户、按 `#标签` 浏览、热门标签
- **作者页**：画廊自定义画集（子画廊）、「文章」标签（期刊 / 文学投稿）
- **富文本简介**：作品描述支持链接 / 加粗 / 表情 / 内嵌图片（tiptap + HTML）
- **打开链接**：粘贴 DeviantArt 链接直达作品 / 作者
- **下载**：完成后移入系统「下载」文件夹（移动而非复制）；图片可应用内查看，
  点击可跳回作品详情
- **完整中英双语**：作品详情、首页、设置、诊断、画集等界面本地化

### Fixed

- 修复网页登录态读取：改由内置 WebView 读取 Cookie/CSRF（不再使用会被拦截的
  HTTP），并从 `userinfo` Cookie 读取登录身份，修复登录态上报竞态
- 修复切换账号后残留上一账号内容、双账号并存、WebView 登录页销毁崩溃等问题
- 修复期刊 / 文学投稿 404、缩略图加载慢、文件夹数量错误、网页会话门控
- 修复受限下载回退为「不可用」提示，而不是整页报错
- 修复 Windows CI 构建（`flutter_inappwebview` 与新版 MSVC 不兼容，固定
  `windows-2022`）

### Changed

- 依赖 `dakit_api` 升级到 0.1.8（修复 `formatted_excerpt` 字段拼写，简介回退正确）
- 下载完成后的落盘位置由应用私有目录改为系统「下载」文件夹

## 0.2.4

### Added

- 首页改为内嵌 DeviantArt 网页版首页，个性化推荐与网页版一致
  （`rfy/deviations`）
- 复用首页 WebView 的网页登录态完成 OAuth 授权（网页已登录时无需重输密码）
- 首页原生 AppBar、加载进度条、下拉刷新、Android 返回键接管
- 顶部登录态同步提示条

### Fixed

- 修复 WebView OAuth 回调被顺序 `yield*` 吞掉、导致应用内登录超时的问题
- 修复 release APK 无法构建的问题（回退稳定版 `flutter_inappwebview`，
  并把 AGP 降到 8.13.2 / Gradle 8.14.2 / Kotlin 2.2.20）

### Changed

- 依赖 `dakit_core` / `dakit_api` 升级到 0.1.1

## 0.2.2

### Fixed

- 修复重启后登录态无法恢复、每次都要重新登录的问题
- 修复切换账号后首页仍显示上一个账号内容的问题

### Added

- 设置页「切换账号」入口（登出当前会话并重新授权）

## 0.2.1

- 首个发布版本：OAuth 登录、作品浏览 / 详情 / 大图缩放、后台下载、收藏、
  关注、搜索、双语、代理配置。
